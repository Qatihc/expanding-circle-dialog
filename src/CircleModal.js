import styled from 'styled-components';

const CircleModal = ({ className }) => {
  return (
    <p className={className}>
      asd
    </p>
  )
}

export default styled(CircleModal)`
  color: red;
`;